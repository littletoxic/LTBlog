@inject IDialogService DialogService

<FluentButton BackgroundColor="var(--accent-fill-rest)" OnClick="OpenSiteSettingsAsync" Title="Site settings">
    <FluentIcon Value="@(new Icons.Regular.Size20.Settings())" Color="Color.Neutral" Title="Site settings" />
</FluentButton>

@code {
    private IDialogReference? _dialog;

    private async Task OpenSiteSettingsAsync() {
        //DemoLogger.WriteLine($"Open site settings");
        _dialog = await DialogService.ShowPanelAsync<SiteSettingsPanel>(new() {
            ShowTitle = true,
            Title = "网站设置",
            Alignment = HorizontalAlignment.Right,
            PrimaryAction = "OK",
            SecondaryAction = null,
            ShowDismiss = true
        });

        var result = await _dialog.Result;
    }

}